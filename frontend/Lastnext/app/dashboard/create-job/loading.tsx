import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Preparing job form" description="Loading property-scoped work order fields." />;
}
