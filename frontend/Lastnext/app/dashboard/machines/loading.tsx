import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Loading equipment" description="Preparing machine totals, filters, and records." />;
}
