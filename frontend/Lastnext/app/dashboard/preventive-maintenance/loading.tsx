import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Loading preventive maintenance" description="Preparing schedules, filters, and maintenance work." />;
}
