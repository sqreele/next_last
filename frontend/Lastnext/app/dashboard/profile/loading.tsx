import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Loading profile" description="Preparing your account and property profile." />;
}
