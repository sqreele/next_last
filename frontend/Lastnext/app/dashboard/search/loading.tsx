import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Loading search" description="Preparing property-scoped search results." />;
}
