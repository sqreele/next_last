import { PageLoader } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoader label="Loading utility data" description="Preparing consumption filters, totals, and comparisons." />;
}
